spring:
  profiles:
    active: dev

logging:
  file:
    name: logs/wu-${server.port}.logs

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted # 全局逻辑删除字段名
      logic-delete-value: 1 # 逻辑已删除值
      logic-not-delete-value: 0 # 逻辑未删除值
